{
  "name": "bored-alive",
  "version": "1.0.0",
  "description": "Chat in rooms where the objective is to avoid being boring.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alexander Steele",
  "license": "ISC",
  "dependencies": {
    "ejs": "^2.6.2",
    "express": "^4.17.1",
    "socket.io": "^2.5.0"
  }
}
